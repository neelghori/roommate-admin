/** Same name for localStorage + cookie so middleware can detect the session. */
export const ADMIN_ACCESS_TOKEN_COOKIE = "roommat_admin_access_token";

/** Cached name + email after login (localStorage only). */
export const ADMIN_SESSION_PROFILE_KEY = "roommat_admin_session_profile";
